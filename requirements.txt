# Locked dependency set — Phase 1 Foundation (Provider module)
# Pin exact versions once this moves past the sandbox; ranges kept here
# to match the base converter project this was built from.
psycopg[binary]==3.2.3
sqlparse==0.5.1
python-dotenv==1.0.1
faker==26.0.0
PyYAML==6.0.2

# Phase 5: HTTP bridge to ui/console.jsx (see ADR-0006)
fastapi==0.139.2
uvicorn[standard]==0.51.0
httpx==0.28.1

# Phase 5b: auth/RBAC layer (see ADR-0008)
PyJWT==2.7.0
